EasyCeph - deploy ceph easy and fast

Create cluster with first monitor and one MGR daemon

# ec-cluster-create monip=192.168.1.1:6789 clusternet=192.168.1.0/24 osdnet=192.168.2.0/24
# ec-add-mgr prima --start

Deploy monitor on other host (copy configs and deploy second mon and mgr)

# scp /etc/ceph/ceph.conf /etc/ceph/ceph.client.admin.keyring root@otherhost
# ssh root@otherhost
# ec-add-mon monip=192.168.1.2:6789 monname=secunda
# ec-add-mgr secunda --start

Create custom OSD (two OSDs on one disk)
# gdisk /dev/sdb 
// use gdisk to create 4 partitions - two for OSD root and two for data
# ec-part-tool mark sdb 1 osd
# ec-part-tool mark sdb 2 block
# ec-part-tool mark sdb 3 osd
# ec-part-tool mark sdb 4 block
# chown ceph:ceph /dev/sdb?
# ec-osd-create bluestore sdb1 sdb2 --start
# ec-osd-create bluestore sdb3 sdb4 --start